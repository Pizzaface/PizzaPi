---
title: Install PizzaPi
description: Install the PizzaPi CLI and prepare an AI provider for your first session.
---

PizzaPi runs the pi coding agent on your computer. The CLI is available as `pizzapi` and `pizza`.

## Requirements

- Node.js 18+ or Bun 1.x (the package is distributed through npm).
- An AI provider account, subscription, or API key. See [Providers & Models](/PizzaPi/features/providers-and-models/).
- Docker is only needed if you use `pizzapi local` to start a local relay, or self-host the relay separately.

## Install

```bash
npm install -g @pizzapi/pizza
pizzapi --version
```

Or run without installing:

```bash
npx @pizzapi/pizza --help
```

## Prepare provider access

PizzaPi needs provider credentials independently of your relay login. Start `pizzapi`, then use `/login` to authenticate through a provider's supported OAuth flow, or export its API key before starting the CLI (for example `ANTHROPIC_API_KEY`). Use `/model` to select an available model. Credentials are stored in `~/.pizzapi/auth.json`; see [authentication and model selection](/PizzaPi/features/providers-and-models/).

## Where data is stored

PizzaPi stores global settings and provider credentials under `~/.pizzapi/`. The relay API key and relay URL are in `~/.pizzapi/config.json`; provider auth is in `~/.pizzapi/auth.json`; sessions and logs are also kept under this directory. Project-specific configuration lives in `.pizzapi/` in the project. Back up files before resetting or uninstalling.

To remove provider credentials, delete `~/.pizzapi/auth.json`. To remove all PizzaPi local data, stop its processes and remove `~/.pizzapi/`.

## Relay-free and interactive use

An interactive `pizzapi` session does not require the background runner daemon. To work without a relay for one run, use `PIZZAPI_RELAY_URL=off pizzapi` (or `pizzapi --no-relay`). The agent and local session history still work, but browser streaming and remote spawning do not. Set `"relayUrl": "off"` in config to disable it persistently. Use `pizzapi runner` only when you want headless sessions spawned from the web UI.

## Troubleshooting

If the runner repeatedly crashes on Windows, try running it inside WSL2, which uses the Linux runtime. Capture the runner logs and check the current release notes before switching versions; older reports about specific crash addresses and version ranges may no longer apply.

## Next: run a working session

Follow [Your first working session](/PizzaPi/start-here/getting-started/). Choose local relay, an existing relay, or terminal-only mode; authenticate a model, verify a real response, then stop the processes you started.
