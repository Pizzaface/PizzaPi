/**
 * Historical skipped suite retained as documentation.
 * Active DB-isolated replacements live in ./store.db-isolation.test.ts.
 */
import { describe } from "bun:test";

describe.skip("runner association persistence (migrated to store.db-isolation.test.ts)", () => {});
