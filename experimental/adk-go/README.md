# Go Runner — Moved

The Go runner has been extracted to its own repository:

**https://github.com/Pizzaface/go-pi**

This directory previously contained the Go-based ADK runner for PizzaPi.
It was extracted via `git subtree split` with full commit history preserved.
